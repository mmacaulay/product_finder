services:
  # Firebase emulators for local development
  # Run with: npm run emulators (or firebase emulators:start)
  # This compose file is now minimal - emulators run separately via Firebase CLI

  # Uncomment below if you want to run app in Docker
  # app:
  #   build: .
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     - FIRESTORE_EMULATOR_HOST=host.docker.internal:8080
  #     - FIREBASE_AUTH_EMULATOR_HOST=host.docker.internal:9099
  #   depends_on:
  #     - emulators
